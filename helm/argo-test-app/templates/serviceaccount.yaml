{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "argo-test-app.serviceAccountName" . }}
  labels:
    {{- include "argo-test-app.labels" . | nindent 4 }}
{{- end }}
